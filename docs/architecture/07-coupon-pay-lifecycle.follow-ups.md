# 优惠券与订单支付生命周期 Follow-ups

- FU-6d2a90f3：`request_hash` 中 `coupon_id` 的规范化编码（未选券与选券两种输入的字节级编码）需在发布前冻结，并给出跨版本兼容判定（沿用主档"编码冻结"条款的执行方式）。
- FU-4f9c21ab：生命周期任务每轮取批的批量大小（超时待支付订单与过期可用券各取多少行）未定义；扫描正确性不依赖该值（漏块续扫已保证），属调优参数，定默认值时与 `LIFECYCLE_SCAN_INTERVAL_SECONDS` 一并冻结。
